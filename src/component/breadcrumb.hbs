<nav>
    <ol class="breadcrumb">
        {{#each breadcrumb}}
        <li class="breadcrumb-item">
            <a href="{{url}}">{{name}}</a>
        </li>
        {{#if @last}}<li class="breadcrumb-item active">{{name}}</li>{{/if}}
        {{/each}}
    </ol>
</nav>